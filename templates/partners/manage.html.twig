{% extends 'base.html.twig' %}

{% block title %}Partners index{% endblock %}

{% block body %}
    <article class="post">
        <header>
            <div class="title">
                <h2>Gestion des partenaires</h2>
            </div>
            <div class="meta">
                <a href="{{ path('partners_new') }}">Ajouter un partenaire</a>
            </div>
        </header>
        <section>
            <table class="table">
                <thead>
                <tr>
                    <th>Id</th>
                    <th>Nom</th>
                    <th>Lien</th>
                    <th>Description</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for partner in partners %}
                    <tr>
                        <td>{{ partner.id }}</td>
                        <td>{{ partner.name }}</td>
                        <td>{{ partner.link }}</td>
                        <td>{{ partner.description }}</td>
                        <td>
                            <a href="{{ path('partners_edit', {'id': partner.id}) }}">Modifier</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5">Aucun résultat</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </section>
    </article>
{% endblock %}
