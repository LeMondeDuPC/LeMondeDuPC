framework:
    default_locale: fr
    translator:
        enabled_locales: ['fr', 'en']
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - en
        enabled: false
